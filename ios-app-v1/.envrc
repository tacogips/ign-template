use flake

# Load project secrets from the user's kinko vault when available. Generated
# projects stay usable without kinko and must not commit exported values.
if command -v kinko >/dev/null 2>&1; then
  eval "$(kinko direnv export)"
fi
