---
description: Create a git commit
---

Use the Task tool to spawn the `generate-commit` subagent to create a git commit.

The subagent will:
- Analyze all changes (staged and unstaged)
- Generate a detailed commit message following project format
- Stage all files automatically
- Create the commit without requiring confirmation
- **Never include Claude Code attribution**

No arguments are needed for this command.
